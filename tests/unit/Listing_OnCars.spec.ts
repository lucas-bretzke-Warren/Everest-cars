import { shallowMount } from '@vue/test-utils'
import ListingOnCars from '@/components/ListingOnCars.vue'

describe('não sei oque vai aqui', () => {
    test('vrerifica se o ModalForm ficou true', async () => {
        const wrapper = shallowMount(ListingOnCars)

        expect(wrapper)

    })
})